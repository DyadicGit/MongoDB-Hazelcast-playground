// just for me to remember!

/*
package com.dyadic.playground.hazelcastmongo.hazelcastserver.run;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.ApplicationContext;

import java.io.IOException;

@SpringBootApplication
public class CustomerHazelcastServerApplication {
    public static void main(String[] args) throws IOException {
        ApplicationContext applicationContext = SpringApplication.run(CustomerHazelcastServerApplication.class, args);
        CustomerHazelcastServerNode customerHazelcastServerNode = (CustomerHazelcastServerNode) applicationContext.getBean("customerHazelcastServerNode");
        customerHazelcastServerNode.populateWithCustomers();
    }
}
*/
